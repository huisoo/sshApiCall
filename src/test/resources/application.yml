spring:
  #datasource:
  #  url: jdbc:h2:tcp://localhost/~/jpashop
  #  username: sa
  #  password:
  #  driver-class-name: org.h2.Driver

  #jpa:
  #  hibernate:
  #    ddl-auto: create #기존꺼 drop 후 table create
  #  properties:
  #    hibernate:
        #        show_sql: true System.out 이므로 사용하지 않음
  #      format_sql: true

logging.level:
  org.hibernate.SQL: debug #log로 보여줌
#  org.hibernate.type: trace #parameter value를 알려줌